import TableModal from "@/utils/TableModal";
import React from "react";

export default function CustomModal(){



    return    <TableModal data={initial} dataInfo={dataInfo} setInfoList={setInfo} />
}